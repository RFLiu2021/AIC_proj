

// ------------------------------------------------
//    Cell Geometry
// ------------------------------------------------
forall delete_section()

proc celldef() {
  subsets()
  geom()
  biophys()
  geom_nseg()
}

apicNum		=	1
bigDendNum	=	1
dendNum		=	6
myNum		=	10
nodeNum		=	10

create soma, bigDend, dend[dendNum], apic[apicNum]  //, my[myNum], node[nodeNum]

soma { L=30   diam=15 }
apic { L=800   diam=3    nseg=10 }
bigDend { L=500   diam=3   nseg=100 }
//for i=0,5  dend[i] {
//      L=100
//      nseg=10
//      diam=2
//   }

connect apic(0), soma(0)
connect bigDend(0), soma(1)
//soma for i=0,5 {
//      connect dend[i](0), 0.5
//   }



// Group the apartments.
objref all, somatic, bigBasal, apical, allBasal, allDend
proc subsets() { local i
    objref  all, somatic, basal, bigBasal, apical, allBasal, allDend
    somatic = new SectionList()
        soma somatic.append()
    bigBasal = new SectionList()
        bigDend bigBasal.append()
//    allBasal = new SectionList()
//        for i=0,5 dend[i] allBasal.append()       
    apical = new SectionList()
        apic apical.append()
    allDend = new SectionList()
        allBasal allDend.append()
        apical   allDend.append()
 
}


load_file("../mechs/createAxon_idealVEN.hoc")
create_axon()


proc geom() {
}
proc geom_nseg() {
}
proc biophys() {
}


access soma

celldef()
